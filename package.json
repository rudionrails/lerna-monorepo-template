{
  "private": true,
  "name": "lerna-monorepo-template",
  "workspaces": [
    "packages/*",
    "utils/*"
  ],
  "scripts": {
    "build": "lerna run build --no-bail --stream --concurrency 1 -- --clean",
    "lint": "lerna run lint --no-bail --stream --concurrency 1",
    "test": "lerna run test --no-bail --stream --concurrency 1"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
